namespace UserManagment.Domain.Constants;

public static class StudentProfileConstants
{
    /// <summary>Minimum profile completeness (0–100) required to book a tutoring session.</summary>
    public const int MinCompletenessForBooking = 90;
}
